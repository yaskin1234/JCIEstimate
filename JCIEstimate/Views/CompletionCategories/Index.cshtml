@model IEnumerable<JCIEstimate.Models.CompletionCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.completionCategory1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.behaviorIndicator)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.completionCategory1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.behaviorIndicator)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.completionCategoryUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.completionCategoryUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.completionCategoryUid })
        </td>
    </tr>
}

</table>
