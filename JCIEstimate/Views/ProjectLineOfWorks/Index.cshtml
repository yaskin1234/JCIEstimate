@model IEnumerable<JCIEstimate.Models.ProjectLineOfWork>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.scopeOfWork)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LineOfWork.lineOfWork1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Project.project1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.scopeOfWork)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LineOfWork.lineOfWork1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Project.project1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.projectLineOfWorkUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.projectLineOfWorkUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.projectLineOfWorkUid })
        </td>
    </tr>
}

</table>
