@model IEnumerable<JCIEstimate.Models.ExpenseMiscellaneou>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.expenseMiscellaneous)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.expenseMiscellaneousDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.expenseMiscellaneous)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.expenseMiscellaneousDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.expenseMiscellaneousUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.expenseMiscellaneousUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.expenseMiscellaneousUid })
        </td>
    </tr>
}

</table>
