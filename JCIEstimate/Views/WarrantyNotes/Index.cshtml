@model IEnumerable<JCIEstimate.Models.WarrantyNote>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.warrantyNote1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WarrantyIssue.warrantyIssueLocation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.warrantyNote1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WarrantyIssue.warrantyIssueLocation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.warrantyNoteUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.warrantyNoteUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.warrantyNoteUid })
        </td>
    </tr>
}

</table>
