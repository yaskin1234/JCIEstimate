@model IEnumerable<JCIEstimate.Models.Location>

@{
    ViewBag.Title = "Index";
}



<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Project.project1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.location1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.locationDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Project.project1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.location1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.locationDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.locationUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.locationUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.locationUid })
        </td>
    </tr>
}

</table>
