@model IEnumerable<JCIEstimate.Models.ProjectAttachment>

@{
    ViewBag.Title = "Index";
}

<p>
    @if (User.IsInRole("Admin"))
    {
        @Html.ActionLink("Create New", "Create")
    }
    
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Project.project1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.projectAttachment1)
        </th>
        <th>
            Get Attachment
        </th>
        <th>
            @Html.DisplayNameFor(model => model.documentName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Project.project1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.projectAttachment1)
        </td>
        <td>
            @Html.ActionLink("Get Attachment", "GetProjectAttachment", new { projectAttachmentUid = item.projectAttachmentUid })
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.documentName)
        </td>
        <td>
            @if(User.IsInRole("Admin"))
            {
                @Html.ActionLink("Edit", "Edit", new { id = item.projectAttachmentUid });
            }
             
            @Html.ActionLink("Delete", "Delete", new { id=item.projectAttachmentUid })
        </td>
    </tr>
}

</table>
