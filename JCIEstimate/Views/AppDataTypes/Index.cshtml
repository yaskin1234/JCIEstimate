@model IEnumerable<JCIEstimate.Models.AppDataType>

@{
    ViewBag.Title = "Index";
}



<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.appDataType1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.appDataType1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.appDataTypeUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.appDataTypeUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.appDataTypeUid })
        </td>
    </tr>
}

</table>
