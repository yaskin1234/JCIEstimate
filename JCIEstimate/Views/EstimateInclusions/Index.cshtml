@model IEnumerable<JCIEstimate.Models.EstimateInclusion>

@{
    ViewBag.Title = "Index";
}



<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.estimateInclusionID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estimateInclusion1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estimate.notes)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.estimateInclusionID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estimateInclusion1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estimate.notes)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.estimateInclusionUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.estimateInclusionUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.estimateInclusionUid })
        </td>
    </tr>
}

</table>
