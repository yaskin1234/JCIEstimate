@model IEnumerable<JCIEstimate.Models.ControlType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.controlType1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.controlTypeDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.behaviorIndicator)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.controlType1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.controlTypeDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.behaviorIndicator)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.controlTypeUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.controlTypeUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.controlTypeUid })
        </td>
    </tr>
}

</table>
