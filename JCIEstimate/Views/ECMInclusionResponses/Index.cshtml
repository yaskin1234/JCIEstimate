@model IEnumerable<JCIEstimate.Models.ECMInclusionResponse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ecmInclusionResponseID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ecmInclusionResponse1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ECMInclusion.ecmInclusion1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ecmInclusionResponseID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ecmInclusionResponse1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ECMInclusion.ecmInclusion1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ecmInclusionResponseUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.ecmInclusionResponseUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ecmInclusionResponseUid })
        </td>
    </tr>
}

</table>
