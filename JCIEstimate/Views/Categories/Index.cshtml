@model IEnumerable<JCIEstimate.Models.Category>

@{
    ViewBag.Title = "Categories";
}

<h2>Categories</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.category1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.categoryDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.category1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.categoryDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.categoryUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.categoryUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.categoryUid })
        </td>
    </tr>
}

</table>
