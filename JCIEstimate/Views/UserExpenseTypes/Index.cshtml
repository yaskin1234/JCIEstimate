@model IEnumerable<JCIEstimate.Models.UserExpenseType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserExpenseType1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.behaviorIndicator)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserExpenseType1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.behaviorIndicator)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.userExpenseTypeUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.userExpenseTypeUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.userExpenseTypeUid })
        </td>
    </tr>
}

</table>
