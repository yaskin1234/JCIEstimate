@model IEnumerable<JCIEstimate.Models.RfiStatu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.rfiStatus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.behaviorIndicator)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.rfiStatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.behaviorIndicator)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.rfiStatusUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.rfiStatusUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.rfiStatusUid })
        </td>
    </tr>
}

</table>
