@model IEnumerable<JCIEstimate.Models.ProjectUser>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AspNetUser.UserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Project.project1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetUser.UserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Project.project1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.projectUserUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.projectUserUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.projectUserUid })
        </td>
    </tr>
}

</table>
