@model IEnumerable<JCIEstimate.Models.PECCost>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AspNetUser.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Project.project1)
        </th>
        <th>
            Week
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pecTaskUid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PECExpenseType.pecExpenseType1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.quantity)
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetUser.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Project.project1)
        </td>
        <td>
            @Convert.ToDateTime(item.Week.startDate).ToString("yyyy-MM-dd") to @Convert.ToDateTime(item.Week.endDate).ToString("yyyy-MM-dd")
            
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PECTask.pecTask1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PECExpenseType.pecExpenseType1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.quantity)
        </td>        
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.pecCostUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.pecCostUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.pecCostUid })
        </td>
    </tr>
}

</table>
