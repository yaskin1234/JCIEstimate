@model IEnumerable<JCIEstimate.Models.ECMInclusion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ecmInclusionID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ecmInclusion1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ECM.ecmNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ecmInclusionID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ecmInclusion1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ECM.ecmNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ecmInclusionUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.ecmInclusionUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ecmInclusionUid })
        </td>
    </tr>
}

</table>
