@model IEnumerable<JCIEstimate.Models.CalendarDayTask>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.task)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CalendarDay.calendarDayUid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Location.location1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Project.project1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.task)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CalendarDay.calendarDayUid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Location.location1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Project.project1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.calendarDayTaskUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.calendarDayTaskUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.calendarDayTaskUid })
        </td>
    </tr>
}

</table>
