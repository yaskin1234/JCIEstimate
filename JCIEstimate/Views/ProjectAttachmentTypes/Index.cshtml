@model IEnumerable<JCIEstimate.Models.ProjectAttachmentType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.projectAttachmentType1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.behaviorIndicator)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.projectAttachmentType1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.behaviorIndicator)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.projectAttachmentTypeUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.projectAttachmentTypeUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.projectAttachmentTypeUid })
        </td>
    </tr>
}

</table>
