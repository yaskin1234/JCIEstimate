@model IEnumerable<JCIEstimate.Models.EstimateStatu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.estimateStatus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estimateStatusDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.behaviorIndicator)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.estimateStatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estimateStatusDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.behaviorIndicator)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.estimateStatusUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.estimateStatusUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.estimateStatusUid })
        </td>
    </tr>
}

</table>
