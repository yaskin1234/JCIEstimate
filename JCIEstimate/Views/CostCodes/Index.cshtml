@model IEnumerable<JCIEstimate.Models.CostCode>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.costCode1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.costCodeDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.jciCodeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.costCode1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.costCodeDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.jciCodeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.costCodeUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.costCodeUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.costCodeUid })
        </td>
    </tr>
}

</table>
