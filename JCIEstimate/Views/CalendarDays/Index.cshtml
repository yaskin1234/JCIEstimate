@model IEnumerable<JCIEstimate.Models.CalendarDay>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Calendar.aspNetUserUid)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Calendar.aspNetUserUid)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.calendarDayUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.calendarDayUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.calendarDayUid })
        </td>
    </tr>
}

</table>
