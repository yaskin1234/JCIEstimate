@model IEnumerable<JCIEstimate.Models.ECMExclusionResponse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ecmExclusionResponseID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ecmExclusionResponse1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ECMExclusion.ecmExclusion1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ecmExclusionResponseID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ecmExclusionResponse1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ECMExclusion.ecmExclusion1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ecmExclusionResponseUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.ecmExclusionResponseUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ecmExclusionResponseUid })
        </td>
    </tr>
}

</table>
