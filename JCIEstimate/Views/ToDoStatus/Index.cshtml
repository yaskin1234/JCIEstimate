@model IEnumerable<JCIEstimate.Models.ToDoStatu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.toDoStatus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.toDoStatusDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.behaviorIndicator)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.toDoStatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.toDoStatusDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.behaviorIndicator)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.toDoStatusUid }) |
            @Html.ActionLink("Details", "Details", new { id=item.toDoStatusUid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.toDoStatusUid })
        </td>
    </tr>
}

</table>
